@model List<StudentManager.BL.Student>
@{
    ViewData["Title"] = "List of Students:";
}

<h1>@ViewData["Title"]</h1>

    @foreach (var item in Model)
    {
        <p>@item.Name</p>
        <ul>
            <li>@item.Program.Name.ToString()</li>
        </ul>
    }



